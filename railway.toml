[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/_cluster/health?wait_for_status=yellow&timeout=5s"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
requiredMountPath = "/usr/share/opensearch/data"
